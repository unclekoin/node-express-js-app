<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0"
          name="viewport">
    <meta content="ie=edge" http-equiv="X-UA-Compatible">
    <link crossorigin="anonymous" href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css"
          integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" rel="stylesheet">
    <title><%= title %></title>
</head>
<body>
<div class="container pt-5">
    <h1 class="text-center mb-3"><%= title %></h1>

    <form class="mb-3" action="/" method="post">
        <div class="form-group mb-3">
            <input type="text" class="form-control" name="title" required>
        </div>
        <button class="btn btn-primary" type="submit">Create</button>
    </form>
    <% if (notes.length) { %>
        <div class="card">
            <ul class="list-group">
                <% for (let i = 0; i < notes.length; i++) { %>
                    <li class="list-group-item d-flex justify-content-between align-items-center">
                        <span class="list-group-item-text" data-id="<%= notes[i].id %>"><%= notes[i].title %></span>
                        <div class="flex-shrink-0">
                            <button class="btn btn-outline-primary me-2" data-type="edit" data-id="<%= notes[i].id %>">
                                Update
                            </button>
                            <span class="fs-5 px-1" role="button" data-type="remove" data-id="<%= notes[i].id %>">&times;</span>
                        </div>
                    </li>
                <% } %>
            </ul>
        </div>
    <% } else { %>
        <div class="alert alert-warning">Notes not found</div>
    <% } %>
</div>

<script src="/app.js"></script>
</body>
</html>
